<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.leandro.appdit.MainActivity"
    tools:showIn="@layout/app_bar_main"
    android:background="#e6e5e5">

        <ImageView
            android:id="@+id/imageView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingTop="16dp"
            android:src="@drawable/logo_dit"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/editText"
            android:text="@string/departamento"
            android:layout_centerHorizontal="true"
            android:elegantTextHeight="true"
            android:layout_below="@+id/imageView"
            android:textAlignment="center" />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/mail"
            android:id="@+id/btnMail"
            android:drawableLeft="@android:drawable/ic_menu_send"
            android:layout_centerVertical="true"
            android:layout_centerHorizontal="true" />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/llamado"
            android:id="@+id/btnLlamar"
            android:drawableLeft="@android:drawable/ic_menu_call"
            android:layout_below="@+id/btnMail"
            android:layout_marginTop="44dp"
            android:layout_alignLeft="@+id/btnMail"
            android:layout_alignStart="@+id/btnMail"
            android:layout_alignRight="@+id/btnMail"
            android:layout_alignEnd="@+id/btnMail" />

</RelativeLayout>
